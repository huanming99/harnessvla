两个 LIBERO-Pro 单元（LIBERO-Pro cells）的终止状态帧（terminal-state frames）。第一组三联图（triplet）对比了 RLinf 在标准 OBJECT 任务（standard OBJECT task）、任务扰动 OBJECT-PRO 变体（task-perturbed OBJECT-PRO variant）以及 Harness VLA 在同一扰动任务上的表现：当任务描述（task description）把目标（target）重定向，而视觉场景（visual scene）仍然相似时，RLinf 会重复标准任务行为（standard behavior），而不是跟随新的指令（new instruction）。第二组三联图展示了交换扰动 GOAL-PRO 任务（swap-perturbed GOAL-PRO task）中的类似对比：布局变化（layout changes）后，RLinf 仍然盲目地把物体移动到训练时区域（training-time region）；而 Harness VLA 会通过规划器 Π（planner Π）重新 grounding 目标，使用解析 primitive（analytic primitives）完成 staging，并调用 VLA ACT 执行局部接触丰富操作（local contact-rich operation）。
