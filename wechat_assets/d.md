跨 benchmark 的任务完成归因（task completion attribution）。柱状图展示了成功 rollout 中，最终 benchmark 完成谓词（final benchmark completion predicate）是在解析 primitive（analytic primitive，蓝色）之后触发，还是在 VLA primitive（橙色）之后触发。LIBERO 系列任务（LIBERO-family tasks）大多是在 VLA 建立稳定接触（stable contact）后，由解析 primitive 完成最后一步；而 RoboCasa365 和 RoboTwin C2R 包含更多终止阶段的接触丰富操作（terminal contact-rich operations），例如设备操作（fixture actuation）、受约束放置（constrained placement）或双臂物体交互（bimanual object interaction）。
