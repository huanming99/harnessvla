围绕接触丰富阶段（contact-rich phases）进行解析分解（analytic decomposition）的代表性 rollout 帧。上排展示 LIBERO-10-PRO 交换任务（swap task）：智能体（agent）先调用 VLA ACT，并开始朝篮子移动；随后在 MOVE TO 执行过程中检测到 VLA 实际上没有抓住奶油奶酪盒（cream-cheese box）。规划器移动回去，重新调用 VLA ACT；成功抓取后，再用 MOVE TO 和 RELEASE 完成子任务（subtask）。下排展示 RoboCasa STEAMINMICROWAVE 的组合已见任务（composite-seen task）：智能体先用 VLA ACT 成功抓住碗，随后搜索并重定位直到定位到微波炉（localize the microwave），再调用 VLA ACT 把碗放入微波炉，用 MOVE TO 把碗推入，关闭门，最后使用 MOVE TO 和 NAVIGATE TO 按下开关。
