自适应 VLA 调用（adaptive VLA invocation）可以提升多个 benchmark 上的成功率。每个子图都展示累计任务成功率（cumulative task success）如何随每个 episode 允许的最大 VLA primitive 调用次数（maximum number of VLA primitive invocations）变化。蓝色虚线表示对应的冻结策略基线（frozen-policy baseline），灰色虚线表示允许所有规划器选择调用（planner-selected invocations）时的完整 Harness VLA 性能。在 LIBERO-Pro、RoboCasa365 和 RoboTwin C2R 上，成功率在前几次 VLA 调用后迅速上升，随后逐渐接近完整 Harness VLA 的结果；这说明重复的、由规划器 staging 后的调用（planner-staged invocation）是有用的，但整体上仍然是稀疏的（sparse）。
